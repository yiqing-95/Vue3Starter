import { defineComponent, ref } from 'vue';
export default defineComponent(() => {
    // 传入 setup 函数

    const render = () => {
        return <div>标题66688{props}</div>;
    };
    return render;
});

